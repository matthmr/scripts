#!/usr/bin/sh

file=/tmp/.wp.png

if [[ ! -f "$file" ]]; then
  if [[ -s /tmp/.wp ]]; then
    source="$(cat /tmp/.wp)"
  else
    source=~/.wp
  fi

  ffmpeg -y -i "$source" -vf 'scale=1920x1080' "$file" -v 0
fi

i3lock -i "$file" -e -f -b -n -t -p default
rm $file
