#!/usr/bin/sh

PACMANDIR=/mnt/ssd/pacman-artix

PACMAN=/usr/bin/pacman
CONFIG=$PACMANDIR/pacman.conf
HOOKDIR=$PACMANDIR/hooks

ARGS=("--config $CONFIG --hookdir $HOOKDIR")

exec $PACMAN --color=always $@ $ARGS
