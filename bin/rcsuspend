#!/usr/bin/sh

case $1 in
  '-h'|'--help')
    echo "Usage:       rcsuspend"
    echo "Description: Suspends the system"
    exit 1;;
esac

TERM=st

export XINITSLEEP="doas"
export XINITSLEEPARGS="/home/mh/.local/bin/s2ram"

exec $TERM -e zsh
