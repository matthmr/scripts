#!/bin/sh

SUDO=doas
SHUTDOWN=openrc-shutdown

AS=${0##*/}
AS=${AS/rcn/}

case $AS in
	'shutdown')
		$SUDO $SHUTDOWN -p now;;
	'reboot')
		$SUDO $SHUTDOWN -r now;;
	*) # fallback
		$SUDO $SHUTDOWN -p now;;
esac
